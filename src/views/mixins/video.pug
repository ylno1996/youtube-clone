mixin video(video)
    div     
        h4
            a(href=`/videos/${video.id}`, class="video-small")
                img(src="/uploads/img/thumb.webp", alt="썸네일" , class="thumb")
                span=video.title
        p=video.description
        small=video.createdAt
        p 
            small 조회수 
            small=video.meta.views 
        hr 